{
  "name": "@vector-metis/typescript-sdk-workspace",
  "private": true,
  "packageManager": "pnpm@12.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "pack": "pnpm --filter @vector-metis/server-sdk pack --pack-destination dist && pnpm --filter @vector-metis/browser-sdk pack --pack-destination dist"
  }
}
